export default function Navbar({username}){
    return (
        <header>
            <nav>
                <h1>
                    Hiof:D
                </h1>
            </nav>
            <span>{username}</span>
        </header>
    )
}